# fizzbuzz

TODO (build order step 6-7, once IF/THEN and FOR/NEXT exist): add
`program.bas` (classic FizzBuzz 1..N using MOD) and `expected.txt` (its
exact stdout). No `input.txt` needed — this program doesn't use INPUT.

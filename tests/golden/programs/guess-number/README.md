# guess-number

TODO (build order step 9, once INPUT exists): add `program.bas` (a
number-guessing game using RND — MUST call `RANDOMIZE <fixed-seed>` for
deterministic test output, per DIALECT.md's RND locked default),
`input.txt` (scripted guesses, one per line), and `expected.txt`.

// `basic2js run <input.bas>` — the primary "just execute it" path.
//
// TODO (build order step 5, polished step 19): compile in-memory (no disk
// write) and call run(new NodeRuntime()) directly in-process. Support
// --emit-ast / --emit-steps debug flags to dump intermediate compiler
// output instead of executing.

export {};

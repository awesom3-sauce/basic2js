#!/usr/bin/env node
// basic2js CLI entry point.
//
// TODO (build order step 5, polished step 19): wire up `convert` and `run`
// subcommands (see ./commands/) using a small CLI-arg-parsing library (e.g.
// `cac`), plus --help and correct process exit codes.

export {};

// The small embedded support-library text (coercion/string/math/formatting
// helpers) inlined at the top of every generated output.js, so emitted
// files have zero import dependencies on this compiler's own source tree.
//
// TODO (build order step 5, expanded through step 14): export a PRELUDE
// string (or a function producing one) assembled from src/runtime/shared/*'s
// logic, kept in sync with — but textually independent of — those modules.

export {};

// Expression parsing (Pratt/precedence-climbing) — literals, variable/array
// refs, unary/binary operators, function/FN calls.
//
// TODO (build order step 2, expanded step 6 for full precedence, step 13 for
// FN calls): export function parseExpression(tokens, pos, minBindingPower): ...
// Consumes precedence.ts's binding-power table.

export {};

// Pratt-parser binding-power table for BASIC expression parsing.
//
// TODO (build order step 6): define binding powers for BinOp per classic
// BASIC precedence (highest to lowest, roughly): ^ ; unary - ; * / ; \ (int
// div) ; MOD ; + - ; comparisons (= <> < > <= >=) ; NOT ; AND ; OR.
// Document the exact table in DIALECT.md once finalized.

export {};

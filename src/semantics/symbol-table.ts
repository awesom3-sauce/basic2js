// Tracks declared variables/arrays and their TypeSuffix across a Program, so
// the analyzer can catch string/number mismatches at compile time wherever
// the suffix is syntactically known.
//
// TODO (build order step 15): export class SymbolTable with methods to
// record/query a variable or array's suffix and dimensionality.

export {};

// Vite config for the web UI.
//
// TODO (build order step 17): import { defineConfig } from 'vite'; import
// react from '@vitejs/plugin-react'; export default defineConfig({ plugins:
// [react()], resolve: { alias: { '@core': path.resolve(__dirname, '../src')
// } } }); — the alias lets web/src/engine import the compiler core's
// src/index.ts without a package publish step.

export {};

// Dropdown/list to load a bundled sample .bas program (from ../../examples,
// which mirrors tests/golden/programs) into the CodeEditor. Presentation +
// local wiring only.
//
// TODO (build order step 17/18): props { onSelect(source: string): void }.

import styles from "./ExamplesMenu.module.css";

export {};

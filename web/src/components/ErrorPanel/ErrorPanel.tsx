// Displays compile-time diagnostics or a BasicRuntimeError, with the BASIC
// line number surfaced prominently. Presentation only.
//
// TODO (build order step 17): props { error: BasicRuntimeError | Diagnostic[] | null }.

import styles from './ErrorPanel.module.css';

export {};
